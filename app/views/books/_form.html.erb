<div class="tile box is-vertical is-5">
  <div class="p-5">
  <h1 class="title"><%= action_controller_title %></h1>  
    <%= form_with(model: book, class:"form") do |form| %>
      <% if book.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

          <ul>
            <% book.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= form.label :title, class:"label" %>
        <%= form.text_field :title, class:"input is-small" %>
      </div>

      <!-- REMOVE THIS -->
      <!--<div class="field">-->
      <!--<%#= form.label :author_id %>-->
      <!--<%#= form.text_field :author_id %>-->
      <!--</div>-->

      <!--Added-->
      <div class="field">
        <%= form.label :author, class:"label" %>
        <div class="select is-small">
          <%= form.collection_select :author_id, Author.order(:name), :id, :name %>
        </div>
      </div>

      <div class="actions control mt-5">
        <%= form.submit "Create #{action_controller_title}", class: "button is-link is-primary is-small" %>
      </div>
  </div>
</div>
<% end %>
